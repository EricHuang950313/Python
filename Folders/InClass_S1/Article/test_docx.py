# This Program ism't complete in function "WriteIn" All of them are comments
'''import tkinter as tk
from functools import partial
import random
from win32com.client import Dispatch, constants
import os
import sys
import math
import win32com

def main():
    
    window = tk.Tk()
    window.geometry("100x80+0+0")
    window.configure(bg="light yellow")
    Button(window)
    window.mainloop()


def Button(window):
    B1 = tk.Button(window,text="Confirm", font=("微軟正黑體", 16), command=partial(WriteIn, window))
    B1.place(x=10, y=10)

def WriteIn(window):

    
             
            


if __name__ == "__main__":
    main()'''